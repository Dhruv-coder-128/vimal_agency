name: Java CI/CD with Maven & Docker

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
      
    - name: Build Docker image
      run: docker build -t vimal-agency .

    # Uncomment and configure these steps for automatic deployment
    # - name: Log in to Docker Hub
    #   uses: docker/login-action@v2
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}
    # - name: Push image
    #   run: |
    #     docker tag vimal-agency ${{ secrets.DOCKER_USERNAME }}/vimal-agency:latest
    #     docker push ${{ secrets.DOCKER_USERNAME }}/vimal-agency:latest
